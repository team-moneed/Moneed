{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "prisma generate && yarn build:web",
    "outputDirectory": "apps/web/.next",
    "installCommand": "yarn install",
    "crons": [
        {
            "path": "/api/cron/shorts",
            "schedule": "0 0 * * *"
        }
    ]
}
